import { Box, Flex } from '@chakra-ui/react';

function Contact(){
  return(
    <Flex bg={"gray.500"} h={'100vh'} w={'full'}  direction={{ base: 'column', sm: 'row' }}
    overflow='hidden'>
  </Flex>
  )
}
export default Contact 