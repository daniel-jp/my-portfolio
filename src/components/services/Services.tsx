import { Box, Flex } from '@chakra-ui/react';

function Services(){
  return( 
  <Flex bg={"red.100"} h={'100vh'} w={'full'} direction={{ base: 'column', sm: 'row' }}
  overflow='hidden'>
</Flex>
  )
}
export default Services