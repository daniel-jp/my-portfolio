import { Box, Flex } from '@chakra-ui/react';

function Portfolio(){
  return(
    <Flex bg={"gray.300"} h={'100vh'}   direction={{ base: 'column', sm: 'row' }}
    overflow='hidden'>
  </Flex>
  )
}
export default Portfolio
