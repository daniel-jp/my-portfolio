import { Box, Flex } from '@chakra-ui/react';

function skills(){
  return(
    <Flex bg={"gray.800"} w={'full'} h={'100vh'} ml={{base:0, md:250}} mr={{base:0, md:3}}  direction={{ base: 'column', sm: 'row' }}
  overflow='hidden'>
</Flex>
  )
}
export default skills
